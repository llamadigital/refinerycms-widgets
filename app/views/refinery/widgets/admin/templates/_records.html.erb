<%= render 'refinery/admin/search_header', :url => refinery.widgets_admin_templates_path %>
<div class='pagination_container'>
  <% if @templates.any? %>
    <%= render 'templates' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
